#include <iostream>  // Required for input and output
#include <string>   // Required for using the string class

int main() {
    // Step 1: Declare and initialize the variables
    std::string name = "Kalvium";  // Store "Kalvium" as the name
    int age = 2;                   // Store 2 as the age

    // Step 2: Print the output
    std::cout << "My name is " << name << " and my age is " << age << " years." << std::endl;

    return 0;  // Indicate that the program ended successfully
}
