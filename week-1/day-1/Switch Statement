import math

class Solution:
    def switchCase(self, choice: int, arr: list) -> float:
        if choice == 1:
            R = arr[0]
            return math.pi * R * R
        elif choice == 2:
            L = arr[0]
            B = arr[1]
            return L * B
        else:
            return 0.0  
solution = Solution()